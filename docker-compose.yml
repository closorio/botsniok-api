services:
  telbot:
    build: .
    env_file: .env
    volumes:
      - ./google_translate_apikey.json:/app/google_translate_apikey.json:ro
    ports:
      - "${PORT:-3000}:3000"
    restart: unless-stopped
